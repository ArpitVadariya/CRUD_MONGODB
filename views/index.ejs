<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Superhero Database</title>
    <link rel="stylesheet" href="../public/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gray-900 text-white min-h-screen flex flex-col items-center py-10"
  >
    <nav
      class="w-full max-w-4xl px-6 py-4 bg-red-800 rounded-lg shadow-lg flex justify-between items-center mb-10 border-b-4 border-yellow-500"
    >
      <h2 class="text-3xl font-bold text-yellow-400">Superhero Hub</h2>
      <a
        href="/read"
        class="text-yellow-300 hover:underline text-lg font-semibold"
        >View Heroes</a
      >
    </nav>
    <div
      class="w-full max-w-md p-8 bg-gray-800 rounded-lg shadow-lg border-4 border-yellow-500"
    >
      <h1 class="text-4xl font-extrabold text-center text-yellow-400 mb-6">
        Add a Superhero
      </h1>
      <form action="/create" method="post" class="space-y-4">
        <div>
          <input
            class="w-full px-4 py-2 bg-gray-700 rounded-lg border border-yellow-400 focus:outline-none focus:ring-2 focus:ring-yellow-500 text-yellow-300 placeholder-yellow-500"
            type="text"
            name="name"
            placeholder="Hero Name"
            required
          />
        </div>
        <div>
          <input
            class="w-full px-4 py-2 bg-gray-700 rounded-lg border border-yellow-400 focus:outline-none focus:ring-2 focus:ring-yellow-500 text-yellow-300 placeholder-yellow-500"
            type="email"
            name="email"
            placeholder="Contact Email"
            required
          />
        </div>
        <div>
          <input
            class="w-full px-4 py-2 bg-gray-700 rounded-lg border border-yellow-400 focus:outline-none focus:ring-2 focus:ring-yellow-500 text-yellow-300 placeholder-yellow-500"
            type="text"
            name="image"
            placeholder="Hero Image URL"
          />
        </div>
        <div>
          <select
            name="universe"
            required
            class="w-full px-4 py-2 bg-gray-700 rounded-lg border border-yellow-400 focus:outline-none focus:ring-2 focus:ring-yellow-500 text-yellow-300"
          >
            <option value="">Select Universe</option>
            <option value="Marvel">Marvel</option>
            <option value="DC">DC</option>
          </select>
        </div>
        <div class="text-center">
          <input
            type="submit"
            value="Add Hero"
            class="w-full px-4 py-2 bg-red-600 hover:bg-red-700 text-white font-bold rounded-lg cursor-pointer transition-all border-2 border-yellow-500"
          />
        </div>
      </form>
    </div>
  </body>
</html>
