<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Superhero Database</title>
    <link rel="stylesheet" href="../public/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gray-900 text-white min-h-screen flex flex-col items-center py-10"
  >
    <nav
      class="w-full max-w-4xl px-6 py-4 bg-red-800 rounded-lg shadow-lg flex justify-between items-center mb-10 border-b-4 border-yellow-500"
    >
      <h2 class="text-3xl font-bold text-yellow-400">Superhero Hub</h2>
      <a href="/" class="text-yellow-300 hover:underline text-lg font-semibold"
        >Add Super Hero</a
      >
    </nav>
    <div
      class="w-full max-w-5xl p-8 bg-gray-800 rounded-lg shadow-lg border-4 border-yellow-500"
    >
      <h1 class="text-4xl font-extrabold text-center text-yellow-400 mb-6">
        Superheroes
      </h1>
      <div
        class="users grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 justify-center"
      >
        <% if(users.length > 0){ %> <% users.forEach(function(user){ %>
        <div
          class="user w-60 p-4 bg-gray-700 rounded-lg shadow-md border-2 border-yellow-400 transform hover:scale-105 transition-transform"
        >
          <div
            class="w-full h-60 rounded-lg overflow-hidden border-b-4 border-yellow-500"
          >
            <img
              class="w-full h-full object-cover object-top"
              src="<%= user.image %>"
              alt="Superhero Image"
            />
          </div>
          <h3 class="text-xl font-bold text-yellow-300 mt-4">
            <%= user.name %>
          </h3>
          <h5 class="text-yellow-500 text-sm italic"><%= user.email %></h5>
          <div class="flex gap-3 mt-4 justify-center">
            <a
              class="text-blue-400 hover:underline"
              href="/edit/<%= user._id %>"
              >Edit Hero</a
            >
            <a
              class="text-red-500 hover:underline"
              href="/delete/<%= user._id %>"
              >Delete Hero</a
            >
          </div>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-yellow-500 text-center text-xl">
          No Superheroes Found
        </h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
