<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update Superhero</title>
    <link rel="stylesheet" href="../public/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gray-900 text-white flex justify-center items-center h-screen"
  >
    <div
      class="w-full max-w-md p-8 bg-gray-800 rounded-lg shadow-lg border-4 border-yellow-500"
    >
      <nav
        class="mb-6 flex justify-between items-center pb-4 border-b border-yellow-500"
      >
        <a
          href="/"
          class="text-yellow-300 hover:underline text-lg font-semibold"
          >Back to Home</a
        >
        <a
          href="/read"
          class="text-yellow-300 hover:underline text-lg font-semibold"
          >View Heroes</a
        >
      </nav>
      <h1 class="text-2xl font-bold text-center text-yellow-400 mb-6">
        Update Superhero
      </h1>
      <form action="/update/<%= user._id %>" method="post" class="space-y-4">
        <div>
          <input
            class="w-full px-4 py-2 bg-gray-700 rounded-lg border border-yellow-500 focus:outline-none focus:ring-2 focus:ring-yellow-400"
            type="text"
            name="name"
            placeholder="Enter Name"
            value="<%= user.name %>"
            required
          />
        </div>
        <div>
          <input
            class="w-full px-4 py-2 bg-gray-700 rounded-lg border border-yellow-500 focus:outline-none focus:ring-2 focus:ring-yellow-400"
            type="email"
            name="email"
            placeholder="Enter Email"
            value="<%= user.email %>"
            required
          />
        </div>
        <div>
          <input
            class="w-full px-4 py-2 bg-gray-700 rounded-lg border border-yellow-500 focus:outline-none focus:ring-2 focus:ring-yellow-400"
            type="text"
            name="image"
            placeholder="Enter Image URL"
            value="<%= user.image %>"
          />
        </div>
        <div class="text-center">
          <input
            type="submit"
            value="Update Superhero"
            class="w-full px-4 py-2 bg-yellow-500 hover:bg-yellow-600 text-white font-semibold rounded-lg cursor-pointer transition-all"
          />
        </div>
      </form>
    </div>
  </body>
</html>
